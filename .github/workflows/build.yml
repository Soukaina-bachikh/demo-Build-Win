name: Build 4D Project

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: [self-hosted, Windows, X64]

    env:
      TOOL4D_PATH: C:\Users\Admin\Downloads\tool4d_win\tool4d\Tool4D.exe

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build with Tool4D (live logs + save to file)
        shell: cmd
        run: |
          %TOOL4D_PATH% --project "C:\Users\Admin\Desktop\demo-Build4D-Win\Project\demo-Build4D-Win.4DProject" --dataless --skip-onstartup --startup-method "buildMethod" 2>&1 > build_output.txt 2>&1 `
          type build_output.txt

      - name: Verify Build Result
        shell: powershell
        run: |
          Write-Host "🔍 Checking build output..."
          $log = Get-Content build_output.txt -Raw
      
          if ($log -notmatch '✅ All builds succeeded!') {
            Write-Error "❌ Build failed. Pattern not found in output."
            exit 1
          } else {
            Write-Host "✅ Build succeeded. Pattern confirmed."
          }
      
